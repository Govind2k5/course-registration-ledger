# Node dependencies (CRITICAL: Never upload this)
node_modules/

# Hardhat compiled files and caches
artifacts/
cache/
typechain/
typechain-types/

# Environment variables (CRITICAL: Hides your private keys if you add them later)
.env

# Coverage and testing reports
coverage/
coverage.json
gasReporterOutput.json

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db